---- V1.2 ----
--New
  Append function 'parent' to allow to get the parent node
  Append a config system to handle specfic siturasion
  Append option on.click.cursor to set cursor properly when you call elemen.on("click", function(){});
  Append 'length' to element. Now you can easy get how many nodes there exists.
--Change
  element.append now accept string as argument. It will convert it to text node and append it to the dom
---- V1.1 ----
--News
  Append method 'create' to element to easy create a new dom will like as: element(document.createElement("div")) -> element.create("div");
  Append method 'attribute' where you can set or set a doms attribute
--Change
  Changed the hole script so you can extens the class: EM.prototype.name = function(){};
  element.append now accept element as dom
